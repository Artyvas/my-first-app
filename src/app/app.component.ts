import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
  // styles:[`
  //   h3 {
  //     color: green;
  //   }
  // `]
  // This is a way to use styles in-line instead of via StyleSheetList. It is an array since you can use multiple stylesheets
})
export class AppComponent {
}
